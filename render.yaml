services:
  - type: web
    name: ethscope
    runtime: node
    plan: free
    buildCommand: cd backend && npm install
    startCommand: node backend/server.js
    envVars:
      - key: ETHERSCAN_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
